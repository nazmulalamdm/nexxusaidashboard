services:
  nexus-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexus-ai-dashboard
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production